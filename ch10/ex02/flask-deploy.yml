apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-flask
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: flask-web-deploy	
  template:
    metadata:
      labels:
        app.kubernetes.io/name: flask-web-deploy
    spec:
      containers:
      - name: nginx-f		
        image: karma999/mynginxf_ch10:0.3
        ports:
        - containerPort: 80

      - name: flask-web	
        image: karma999/myflask_ch10:0.2
        ports:		
        - containerPort: 8001	


